# Runtime baseline for `make check-quality` - see Makefile.
#
# MS_PER_FIXTURE: benchmark_optimal_solutions' own "Runtime: ... ms/fixture" line.
# Informational only: a >2x jump warns, it never fails a deploy, because wall-clock
# time varies by machine far more than any real regression would.
#
# The accuracy gate does NOT live here. It is per-fixture, in quality_baseline.csv
# beside this file, because no single number over a corpus that keeps gaining hard
# fixtures can tell a real regression apart from new data - measured, see the
# quality-gate section in src/bin/benchmark_optimal_solutions.rs.
MS_PER_FIXTURE=255.0
