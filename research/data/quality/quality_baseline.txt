# Baseline compared against by `make check-quality` / `make deploy` - see Makefile.
# TOTAL_MISMATCHES: benchmark_optimal_solutions' first TOTAL row (mismatches vs. the
# human-authored ground truth in src/test/data/diffs/) - a hard gate, since this is
# algorithm-only and not machine-dependent: deploy fails if this number goes up.
# MS_PER_FIXTURE: benchmark_optimal_solutions' own "Runtime: ... ms/fixture" line -
# informational only (a >2x jump warns but doesn't fail deploy), since wall-clock time
# varies by machine.
# Update deliberately via `make update-quality-baseline` after a reviewed improvement,
# not automatically as a side effect of every deploy.
#
# NOTE: this gate is a ratchet on the raw all-node mismatch total. It is deliberately NOT the
# project's quality *goal*, which is stated in visible nodes (README's "Accurate" principle:
# 90% of test cases with zero mismatched visible nodes, 99% within 4% of visible nodes).
# The two can move independently - a change can leave TOTAL_MISMATCHES flat while fixing or
# breaking visible ones. The gate stays on the raw total because it is a single cheap number
# that cannot silently regress; measure progress toward the goal with the benchmark's own
# `Vis Mism` column, not with this file.
TOTAL_MISMATCHES=2808
MS_PER_FIXTURE=1114.5
