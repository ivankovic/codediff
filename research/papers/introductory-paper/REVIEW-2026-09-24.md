# Hand-annotated review of `main.pdf` — 2026-09-24

The author marked the 14-page PDF of 2026-09-23 in red pen. This file records every mark, what
was done, and the one that is a measurement job rather than an edit. The scan lives outside the
repository; the marks are transcribed here so the list stands on its own.

**STATUS: every prose mark is applied and the paper builds at 12 pages. One mark (R13, the 30%
Unknown share in Figure 2) is answered at the foot of this file and is a corpus re-measurement,
not a text edit.**

The largest change reverses one made six days earlier. The 2026-09-18 review by the second author
(`REVIEW-2026-09-18-fraser.md`, F2 and F4) replaced the introduction's four research-question
groups with four *properties* a diff must have (Expressible, Agreed, Timely, Faithful on screen),
and threaded them through every section opener. This review's note under those bullets is *"I
don't like these new categories. Look into git history and go back to RQ groups."* The RQ-group
list is restored from `0a51541d^`, and every sentence that named a property is either struck on
the same pages or rewritten below. The RQ/RA numbering never changed, so nothing downstream moved.

---

## Page 1 — abstract

| # | mark | done |
|---|---|---|
| R1 | *"where no ~~reader~~ can see"* → "user" | yes |
| R2 | *"A ~~single~~ whole-tree tree-edit-distance computation"* | yes |
| R3 | *"19,798 stratified-sampled code file pairs"* → "code file pairs stratified-sampled by size" (margin: "size") | yes |
| R4 | strike *"Because no two tools report a diff in the same form, we compare them on the share of changes each renders perfectly, marking exactly the lines the human mapping marks."* | yes |
| R5 | *"and plain line-based diff renders 54%"* → "line-based diff, the most popular tool, renders only 54%" (margins: "most popular tool", "only!") | yes |
| R6 | *"~~Finally,~~ as a tool contribution, we present CodeDiff, which combines existing state-of-the-art algorithms"* + "into one robust tool" | yes |
| R7 | strike *"on the same basis as those ten configurations,"* | yes |
| R8 | *"It completes 99.95% of the 442,530 file modifications in the corpus's recent history without a panic."* → "We tested it on 442,530 diffs in 7,400 repositories and it completes 99.95% of diffs successfully." | yes — `\RobustnessFullAttempted{}` diffs in `\NumRepos{}` repositories, `\RobustnessFullCompletedPct{}`% successfully |

## Page 2 — introduction

| # | mark | done |
|---|---|---|
| R9 | The "What makes a diff good…" paragraph and the four property bullets: first annotated (margin "In this paper we explore the following four properties a good diff tool must have", "user", "algorithmically", "Intuitive" for Faithful on screen), then the note at the foot of the page: **"I don't like these new categories. Look into git history and go back to RQ groups."** | yes — the four-group list from `0a51541d^` is back verbatim, with the Nielsen footnote on RQ2; the property bullets and their margin edits are moot |
| R10 | strike *"We do not assume the answer. In particular, we do not take it as given that a syntax-aware diff serves a reader better than a line-based one. That is what RQ4.1 measures … reports where each kind does better."* | yes |
| R11 | strike *"Section 8 then reads every answer as a design decision in a tool."* | yes |
| R12 | Contributions: *"the optimal map between the two ASTs ~~is available~~"* → "was found"; *"for comparison with ~~an independently authored~~ reference"* → "prior" | yes — "was found"; "for comparison with prior work" |

R9 undoes F2/F4's introduction. F2's other half — Table 4 "What each empirical result decided in
CodeDiff's design" and the phases each naming the result they apply — is untouched; only the
sentence in Section 8 that framed it ("not a new matching algorithm … every step a consequence of
a measurement above") was struck, see R38.

## Page 3 — background and Section 3.1

| # | mark | done |
|---|---|---|
| R13 | *"so it ~~stays~~ fast"* → "is" | yes |
| R14 | strike, after the largest file's node count: *"three orders of magnitude between the typical file and the tail … every file of that order is one, which Section 8 returns to."* | yes |

## Page 4 — Sections 3.1–3.3

| # | mark | done |
|---|---|---|
| R15 | Figure 2, circling the 30% Unknown bar: **"Why is this still 30%? Didn't we improve the script?"** | measurement — see the foot of this file |
| R16 | strike *"Over every file r is 0.4702: the largest one percent are the generated tables above … follows its largest points."* | yes |
| R17 | *"Our results are broadly aligned with existing research. Arafat and Riehle report"* — margin "them" | yes — "aligned with existing research, among them Arafat and Riehle, who report" |
| R18 | *"~~The tail is the opposite extreme rather than a larger fraction of the same shape: by~~ the 99th percentile an edit rewrites the file entirely"* — margin "In" | yes — "In the 99th percentile, an edit rewrites the file entirely" |
| R19 | *"so that our own mappings can be ~~read~~ against"* → "compared"; strike *"Section 3.4 describes that oracle and reads the two annotations against each other."* | yes |
| R20 | *"for every node of the ~~before-~~AST, which node of the ~~after-~~AST"* — margin "other"; then strike everything from *"That is the same object an AST diffing algorithm produces…"* through *"…the closest expressible solution is recorded with a note saying what it misses."* | yes — the paragraph is now the definition, the painting sentence and the annotators' experience |

R20 removes the annotation-tool description F5 asked for on 2026-09-18. The tool is still named
in Threats to Validity ("a dedicated annotation tool with no tool output shown") and published
with the corpus.

## Page 5 — Section 3.4

| # | mark | done |
|---|---|---|
| R21 | heading *"An independently authored oracle"* → **"Cross-validation with existing datasets"** | yes |
| R22 | strike *"Every accuracy figure in this paper is scored against ground truth this paper's authors wrote."* and *"is the answer to the obvious objection. It"* | yes |
| R23 | *"~~The oracle's unit is an~~ Eclipse JDT node~~s~~ and ours ~~is a~~ tree-sitter node"* — margin "Their prior art uses" | yes — "Their prior art uses Eclipse JDT nodes and ours tree-sitter nodes" |
| R24 | strike *"One caveat belongs to that dataset and not to the other three: … not an estimate over the 996."* | yes |
| R25 | strike *"Read against our own annotation rather than against any tool, the oracle is the only check in this paper that does not depend on our annotation discipline at all."* | yes |
| R26 | strike *"The control that makes those figures readable is CodeDiff over the same units … at statement level."* | yes |

## Page 6 — Sections 3.4, 4 and 5

| # | mark | done |
|---|---|---|
| R27 | strike the two paragraphs *"The difference between the two readings is mostly precision…"* and *"That reading is bounded by which units are solved…"* | yes |
| R28 | strike Section 4's opener *"A diff that cannot say what happened is wrong before any search begins … before any tool is involved."* | yes |
| R29 | *"Because no ~~tool~~ can possibly solve these diffs fully"* — margin "existing"; strike *"In the datasets, the nodes are left unmatched for now."* | yes |
| R30 | *"matching the two leads to an optimal tree edit distance"* — margin "returns" | yes — "matching the two `return`s" |
| R31 | Section 5's opener: strike *"A correct diff that arrives late is a diff the reader has stopped waiting for."*; *"~~tests~~"* → "explores"; strike *"alone, so that the cost of correctness is known before any tool's shortcuts are measured against it"* | yes — "This section explores the speed of the exact algorithm." |

R26 and R27 together remove the CodeDiff-against-oracle control and its interpretation; the
`\OracleHumanCodeDiff*` macros stay generated and are now unused, and Threats to Validity no
longer says the section reads "the tool over the same units" (R43). Section 3.4 keeps the
resolution rate, the four-dataset sentence and the human-versus-oracle agreement figures.

## Page 7 — Section 6

| # | mark | done |
|---|---|---|
| R32 | strike *"The Agreed and Faithful-on-screen properties fail in the same way: the tool shows one answer where more than one is correct, and the reader has no way to know."* | yes — "This section measures how often a change has more than one correct answer, first in the mapping and then in its rendering." |

## Page 8 — Figure 4, RQ3.3 and Section 7's opener

| # | mark | done |
|---|---|---|
| R33 | Figure 4: **"Make CD visually distinct (e.g. dash-dot)"** | yes — `distributions_report.py` draws the CodeDiff curve dash-dotted as well as heavier; the caption says so |
| R34 | *"so a node ~~of the second kind~~"* → "structural"; *"a diff that is ~~still~~ correct"*; *"~~the same~~ tool scores badly"* → "such" | yes |
| R35 | *"any paper reporting one should ~~say~~ which"* → "state"; *"This paper reports both ~~wherever it reports an accuracy figure~~."* | yes |
| R36 | strike Section 7's *"The sections above measure the problem; this one asks whether the tools that exist have the four properties of Section 1."* and *"so that accuracy and cost can be read together rather than one tool at a time. Two questions follow."* | yes |

## Page 9 — Section 7 and Section 8's opener

| # | mark | done |
|---|---|---|
| R37 | *"in a form ~~the ground truth~~ can be compared ~~with~~ directly"* — margin "that"; *"grammar set ~~matches~~ 730"* — margin "can process"; *"differ ~~so much~~"*; *"Perfect means ~~literally~~ zero mismatched lines. ~~A diff a reader can trust on sight is categorically different from one that is merely close.~~"*; "reader" → "developer" (twice) | yes |
| R38 | strike *"and the spread within a fixture survives into the number instead of being averaged away before it is ever read"* | yes |
| R39 | strike Section 8's *"CodeDiff is not a new matching algorithm: … every step of that order is a consequence of a measurement above."*; *"Two of those consequences are numeric targets rather than design decisions, and the paper states them as such:"* → **"CodeDiff has two targets:"** | yes — the opener now points at Table 4 directly |
| R40 | *"for 99.99% of real-world ~~commits~~"* → "changes" | yes — and the Timely-property clause became "RQ2's one second applied to the population of real changes" |

## Pages 10–12 — Section 8

| # | mark | done |
|---|---|---|
| R41 | Phase 2: strike *", and each is what a reader marks"* | yes |
| R42 | Phase 5: *"Move is the operation ~~the introduction argues a diff needs and line-based~~ tools lack"* — margin "existing" | yes — "Move is the operation existing tools lack" |
| R43 | Result: strike *"Where it falls short is where every tool falls short: the Full list and the largest files. The rest of this section is the evidence for that sentence, then its limits."* | yes |
| R44 | Accuracy: strike from *"and it moves only to 0.323% on the 651-fixture subset every tool scored"* through *"…order the datasets the same way."* | yes — one sentence keeps Table 5 referenced: "Table 5 splits the result by dataset." |
| R45 | strike the whole **Limits** paragraph | yes |

R45 removes the paragraph F6 asked for on 2026-09-18 ("what the result does not show"). Its three
points survive elsewhere: the 1,316 ms maximum is still in Table 3's Max column, the Robust
ceiling is still stated in Threats to Validity, and the in-process timing asymmetry is still in
Table 3's caption and in Threats.

## F4 revisited, the same day: why diff quality matters, from the literature

Restoring the RQ groups (R9) reopened the point behind the second author's F4: the introduction
asserted that diff quality shapes review and never said why. The author's answer was to expand
outward rather than restructure, so the introduction now carries two evidence paragraphs between
its opening and the line-based-diff paragraph, and `references.bib` gained seventeen entries
(metadata from doi.org/Crossref, not transcribed). Three strands:

| strand | evidence cited |
|---|---|
| understanding the change is the work of review | Bacchelli & Bird 2013 (Microsoft: understanding is the main challenge); Tao et al. 2012 (engineers ask for structure, not text deltas) |
| presentation alone moves review outcomes | Fregnan et al. 2022 and Rahman et al. 2026 (file position: 64% lower odds of finding a defect in the last file); Alcocer et al. 2023 (unified vs. split view, eye-tracking); Herzig & Zeller 2013, Barnett et al. 2015, di Biase et al. 2019 (tangled changes and decomposition, controlled experiment); Baum et al. 2019 and Gonçalves et al. 2022 (working memory predicts finding delocalised defects; cognitive load) |
| time and quality, and the diffs that show moves | Sadowski et al. 2018 (Google), Kononenko et al. 2016 (Mozilla), McIntosh et al. 2016 (review participation and defects), Kudrjavets & Rastogi 2023 (speed matters), Meta 2022 (satisfaction falls with P75 review time); Ge et al. 2017 and Brito & Valente 2021 (refactoring-aware diffs lower cognitive effort); Decker et al. 2020 (srcDiff's deltas judged more understandable than the cheapest tree edit) |

The paragraphs close on the gap those studies leave: none of them varies the correspondence the
diff draws, which is what the four RQ groups measure. Three of the seventeen were read from
abstracts or citations only (Alcocer 2023, Rahman 2026, Ge 2017); check them against the full
text before submission. The paper is 13 pages with them.

## Not marked, changed anyway

- `doesn't` → `does not` in Section 4 (STYLE.md: no contractions).
- Threats to Validity, internal validity: "Section 3.4 reads our annotation, and the tool over
  the same units, against ground truth…" → "compares our annotation against ground truth…",
  since R26 removed the tool half.

---

## R15 — why Figure 2 still shows 30% Unknown

The classifier *was* improved: `src/code/tip.rs`'s tables were expanded on 2026-09-13/14
(`b7c4b19a`), and its own doc comment recorded the dry-run effect over the Full-list database as
29.3% unclassified → 6.6%. What never happened is writing that back: the `tip` column of
`/var/tmp/research/full/stats.sqlite` is what `file_stats` stored at measurement time
(2026-09-07), and the only run since (2026-09-20) re-processed the files above 1 MiB and nothing
else. `plots/tips.png` is rendered from that column, so it still shows the 2026-09-07
classification.

`make reclassify-tips MODE=full` (dry run, 2026-09-24) over the 7,045,754 rows:

| category | before | | after | |
|---|---|---|---|---|
| Code | 3,890,994 | 55.22% | 4,688,419 | 66.54% |
| Configuration | 84,973 | 1.21% | 176,475 | 2.50% |
| Data | 911,023 | 12.93% | 1,593,317 | 22.61% |
| Documentation | 45,880 | 0.65% | 153,225 | 2.17% |
| Unknown | 2,112,884 | 29.99% | 434,318 | 6.16% |

1,732,406 rows move, 770,380 of them from Unknown to Code. That last number is why the write-back
is not the whole fix: a file the classifier now calls Code was never read, so it has no bytes,
lines or node count, and Figure 3's code-file distributions (n = 3,890,994 today) would either
count it as unmeasured or silently change their population. The consistent fix is
`make measure-file-stats MODE=full`, which re-walks the checkouts with the current tables and
measures what it classifies, then `make introductory-paper-empirical MODE=full`.

**Run the same evening.** The re-walk went 18:01–20:54 (2h53m, 7,444 repositories, one
`file_stats` process each, no aborts), then the report. Figure 2 now shows Unknown at 6.2% and
Code at 66.5%, and every empirical macro moved with it; `research/data/corpus_stats/PROVENANCE.md`
("Re-measured with the 2026-09-13 classifier, 2026-09-24") records the run and what changed,
including the 820,078 newly classified code files that have no grammar and therefore count as
zero-node files in Figure 3.
