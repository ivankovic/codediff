# Hand-annotated review of `main.pdf` — 2026-09-26

The author marked the 13-page PDF of 2026-09-25 on the Boox (`boox fetch`, 30 ink marks over pages
1–11). The paper was refreshed to 1217 fixtures and gained srcDiff as an eighth tool between that
PDF and this pass, so every mark was applied to the refreshed text; where a mark's sentence had
already changed, the row says how. The scan lives outside the repository
(`~/.cache/boox/research-papers-introductory-paper-main/`); the marks are transcribed here so the
list stands on its own.

**STATUS: every mark is applied and the paper builds at 14 pages (13 before; the two new authors
take the extra space). V1, V2 and V3 below are resolved.**

Three marks turned out to be about claims that were wrong, not about wording:

* **R19** asked what happens to N:M nodes in the line-based comparison. The paragraph said they are
  excluded from every tool's evaluation. They are not: `benchmark_other` projects each multi-map
  group through one representative pairing (`human_mapping::representative_entries`, members
  paired in source order), and only CodeDiff's node-level accuracy grades a group as correct under
  any of its pairings (`check_group_entry`). The paragraph now says that.
* **R24** asked for the tree beside Figure 5. Drawing it showed the caption and prose were wrong:
  tree-sitter-python parses the before snippet into **one** `boolean_operator`, not two nested
  `binary_expression`s, and the after snippet into two, not three. The example still makes its
  point - either after node can be matched at the same cost and the same rendering - and the
  caption, prose and trees now describe the real ASTs (checked with `ascii_visualizer`).
* **R12** asked to double-check that `diff` uses Myers. It does (gnulib `diffseq.h` names Myers
  1986), but unless `--minimal` is given it applies Eggert's `TOO_EXPENSIVE` heuristic, so the
  script is not always the shortest one. Section 2 says so now, and the introduction's "a shortest
  edit script" became "an edit script".

---

## Page 1 — title, abstract, introduction

| # | mark | done |
|---|---|---|
| R1 | "Add Goran Petrović with Uni Passau affiliation and René Just" (arrow between the first and last author) | yes — both added between Marko Ivanković and Gordon Fraser; see V1 |
| R2 | "text!" → *"a human-painted ~~~~ rendering"* | yes — "human-painted text rendering" |
| R3 | strike "line-based" in *"and line-based diff, the most popular tool"* | yes |
| R4 | "Add speed number in this paragraph" (the CodeDiff contribution paragraph) | yes — "at a median of `\SpeedCodeDiffPFifty{}` ms per diff including its parse" |
| R5 | *"During a review, ~~we~~ developers read the diff"* | yes — and the same sentence in the abstract |
| R6 | "Remove newline. Merge paragraphs!" (first two introduction paragraphs) | yes |
| R7 | *"Controlled experiments show the presentation alone ~~moving~~ affecting the outcome"* | yes |
| R8 | *"rate the reaction time of review tooling as ~~of the first importance~~ very important"* | yes |

## Page 2 — introduction

| # | mark | done |
|---|---|---|
| R9 | strike *"What none of these studies varies is the correspondence … This paper measures that property on real changes."* | yes |
| R10 | *"diffs with a ~~genuine~~ move operation"* | yes |
| R11 | strike *"Section 4 takes RQ1, Section 5 RQ2, Section 6 RQ3 and Section 7 RQ4."* | yes |
| R12 | strike *"Answers to all research questions."* (Evaluation contribution) | yes |

## Page 3 — contributions, background, dataset

| # | mark | done |
|---|---|---|
| R13 | Tool contribution + "based on the collected data" | yes |
| R14 | "Double check diff uses myers!" | yes — see above |
| R15 | "30! Not 25?" at *"7,045,754 files in 30 languages"* | checked, no change — `stats.sqlite` holds 30 distinct languages among files classified as code (JSON, YAML, XML, Markdown included); the fixture corpus has 24 |

## Page 4 — dataset

| # | mark | done |
|---|---|---|
| R16 | *"code files that ~~parse~~ can be parsed"* | yes |
| R17 | "Why different number of langs? explain in text!" (22 Curated vs 24 Full) | yes — the Curated list's repositories hold 2 R and 4 Scala files (`/var/tmp/research/small/stats.sqlite`), so those two languages come from the Full list only |
| R18 | *"a pure random sample ~~would be~~ is heavily slanted"*; strike "fully random" in *"the fully random sample was further augmented"*; "Reorder!" *"The larger of the two sizes was used"* to after the strata; *"~~by hand as well~~"*, *"our ~~own~~ mappings"*, *"Each diff was ~~then~~ solved"*, *"who ~~have each~~ each have"* | yes |

## Page 5 — the Defects4J oracle

| # | mark | done |
|---|---|---|
| R18a | *"so the two ~~meet~~ can be compared only where"* | yes |
| R18b | strike *"That share is the part of the oracle we could not ask, and every rate below is over the part we could."* | yes |
| R18c | *"Over the 113 ~~compilation units (101 cases)~~ changes we have mapped"*; strike the recall in *"agree to 99.65% / 99.51%"* | yes — now over 274 changes after the refresh |

## Page 6 — viability, speed

| # | mark | done |
|---|---|---|
| R19 | "Double check! What do we do with N:M nodes in line based tools?" on *"we used a charitable interpretation … and exclude those nodes from the evaluation"* | yes — the claim was false; see above |
| R20 | "Cite papers from intro!" at *"From an industry perspective, one second is the high end of acceptable"* | yes — kudrjavets-2023 and meta-review-time-2022, the introduction's two industry citations |
| R21 | "How many per lang only bucket?" at the RQ2 stratification | yes — new generated macros from `apted_only_report.py`: up to `\RqOnePerCell` (142) pairs in each of `\RqOneCells` (168) cells, `\RqOneFullCells` (127) full |
| R22 | strike *"and the curve keeps climbing at the budget rather than flattening, so a larger budget would buy more pairs at every size"* | yes |
| R23 | RA2: strike the two bucket clauses → "find the diff for only 46.8% of code pairs" | yes |

## Page 7 — uniqueness

| # | mark | done |
|---|---|---|
| R24 | "Maybe visualise the tree as well?" (Figure 5) | yes — two `forest` trees under the code; see above for what drawing them found |

## Page 8 — tool comparison

| # | mark | done |
|---|---|---|
| R25 | RA4.1: strike everything after the first sentence (the line-algorithm and diffsitter sentences) | yes — the first sentence is now the refreshed srcDiff/difftastic one |

## Pages 9–10 — CodeDiff

| # | mark | done |
|---|---|---|
| R26 | *"each empirical result ~~above~~ decided"* | yes |
| R27 | Fast target *"for 99~~.99~~% of real-world changes"* | yes — `TARGETS["SpeedTargetPct"]` is now 99; see V2 |
| R28 | "Add some space between rows!" (Table 4) | yes — `\addlinespace` between rows |
| R29 | *"avoids paying ~~that computation's cost~~ O(n³) on content that never changed"* | yes |
| R30 | strike *"Move is the operation existing tools lack, so the mapping is not closed until this pass has run."* | yes |
| R31 | *"for the best established ~~configuration~~ tool"* | already so after the srcDiff refresh |
| — | a single dot on page 10 (ink3) | stray, no mark |

## Page 11 — robustness

| # | mark | done |
|---|---|---|
| R32 | strike *"0 timeouts and 0 panics across 24 languages"* → "completes every one." | yes |
| R33 | *"the rest ran past even that five-minute budget"* | yes |

---

## Open

* **V1** — resolved 2026-09-26: the author confirmed René Just's affiliation (University of
  Washington) and gave both new authors' emails.
* **V2** — resolved 2026-09-26: the author confirmed the Fast target of 99% of changes.
* **V3** — resolved 2026-09-26: the author keeps "The annotators are the authors" and the Threats
  wording as they are.
