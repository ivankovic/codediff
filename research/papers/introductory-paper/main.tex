\documentclass[sigconf]{acmart}

%% EVERY number in this paper - prose, tables, and abstract alike - is a LaTeX macro defined in
%% figures/variables.tex, generated by research/analysis/paper_variables.py. Nothing below should
%% contain a bare numeric literal that came from a measurement: a number appearing in both a table
%% and the prose discussing it used to be typed twice, so a data refresh could update one and miss
%% the other. See that script's module doc comment for which blocks are read back from a
%% measurement artifact and which are transcribed with their provenance recorded alongside them.
%%
%% The empirical-study numbers (Table 1, corpus size, bytes-AST correlation) currently reflect the
%% 100-repository `small` sample rather than the eventual full 7,423-repository corpus (slow - see
%% this paper's own README.md for how to swap in a full run's numbers once available).
\input{figures/variables.tex}

%% TODO: replace with the real venue/rights info once a target venue is chosen (see the ACM
%% rights-confirmation email); these are placeholder values from the acmart sample template.
\setcopyright{acmlicensed}
\copyrightyear{2026}
\acmYear{2026}
\acmDOI{XXXXXXX.XXXXXXX}
\acmConference[Conference'26]{Make sure to enter the correct conference title from your rights
  confirmation email}{June 03--05, 2026}{Ludbreg, Croatia}
\acmISBN{978-1-4503-XXXX-X/26/06}

%% TODO: verify/regenerate these against the ACM CCS taxonomy tool (https://dl.acm.org/ccs) once
%% the paper's framing is finalized.
\begin{CCSXML}
<ccs2012>
<concept>
<concept_id>10011007.10011074.10011099.10011102</concept_id>
<concept_desc>Software and its engineering~Software maintenance tools</concept_desc>
<concept_significance>500</concept_significance>
</concept>
<concept>
<concept_id>10011007.10011074.10011092.10011691</concept_id>
<concept_desc>Software and its engineering~Empirical software validation</concept_desc>
<concept_significance>300</concept_significance>
</concept>
</ccs2012>
\end{CCSXML}

\ccsdesc[500]{Software and its engineering~Software maintenance tools}
\ccsdesc[300]{Software and its engineering~Empirical software validation}

\keywords{source code differencing, syntax-aware diffing, empirical software engineering, source code tree edit distance}

\title{CodeDiff: A Fast, Robust, Production-Ready, Syntax-Aware Code Diffing Tool}

\author{Marko Ivankovi\'{c}}
\affiliation{%
  \institution{Universit\"{a}t Passau}
  \city{Passau}
  \country{Germany}
}
\email{marko@ivankovic.me}

\begin{document}

\begin{abstract}
A source-code diffing tool needs an accurate, efficient way to map one abstract syntax tree (AST)
onto another. Decades of research have given the field exact tree-edit-distance
algorithms, Zhang-Shasha and APTED, and source-code-specific heuristics, GumTree, plus a newer
generation of practical, tree-sitter-based tools, difftastic and diffsitter. This paper presents
\textsc{CodeDiff}, a syntax-aware code-diffing tool that builds directly on this line of work,
combining APTED's exact tree edit distance with GumTree's bottom-up and move-recovery heuristics
and Myers' O(ND) sequence diff into one \NumPhasesWord{}-phase matching pipeline.
\textsc{CodeDiff}'s own
contribution is not a new matching algorithm but two engineering disciplines applied on top of
this foundation. First, its performance and robustness targets come from an empirical study of
\NumRepos{} real-world open-source repositories, about \NumFilesMillions{} million files in
\NumLanguages{} languages, not from worst-case asymptotic analysis alone. Second, each optional
heuristic pass ships only if an ablation study shows a measurable accuracy gain on a corpus of
\NumFixtures{}
real-world diffs with human-verified ground truth, so that a technique's published benefit is
confirmed in \textsc{CodeDiff}'s own pipeline rather than assumed to transfer unchanged. On that
same ground-truth corpus, \textsc{CodeDiff} maps \NodeAccuracyPct{}\% of AST nodes correctly, and reaches that
accuracy at a speed and reliability suited to everyday, interactive use—read alongside GumTree,
difftastic, and diffsitter not as a replacement for any of them, but as one way of combining what
each already does well.
\end{abstract}

\maketitle

\section{Introduction}
\label{sec:intro}

Code diffing, the process of comparing two pieces of source code and computing the set of
operations that transforms one into the other, underlies modern version control and, especially,
modern code review. During a review, developers read the diff to see what changed and what stayed
the same, so the quality of the diff directly shapes the quality of the review.

The standard diff used by almost all developers and platforms today is line-based: the file is
treated as a sequence of text lines, and the tool computes a shortest edit script between the two
sequences using three operations, insert, delete, and update. This approach is computationally
cheap and fast even on modest hardware. However, most implementations offer no move operation at
all, and the tools that do support one support it only in limited ways. A whole class of common
changes—extracting code into a reusable function, wrapping a block in a condition, reordering
declarations—is ultimately a relocation of existing code, and without a move operation every such
change must be rendered as a full deletion plus a full insertion. Reconstructing the
correspondence between the deleted and inserted halves is then left to the reviewer, by eye,
which is exactly the kind of mechanical, attention-taxing work humans do least reliably.

One way to compute diffs with a genuine move operation is to compare the code's abstract syntax
tree (AST) instead of its raw text. An AST diff maps nodes of one tree onto nodes of the other; a
move is a mapping whose nodes match but whose ancestry changed. Tree diffing is a well-studied
field, and Section~\ref{sec:background} surveys the work this paper builds on. In practice,
however, tree diffing has seen little adoption, and we argue the central reason is cost: the best
exact tree-diffing algorithms are $O(n^3)$ in tree size, and Section~\ref{sec:problem-space}
measures directly how often a whole-tree computation at real-world file sizes exceeds an
interactive time budget.

This paper presents \textsc{CodeDiff}, a syntax-aware code-diffing tool that combines
established algorithms with rigorous engineering practice to meet explicit performance and
robustness targets. The targets come from an empirical study of real open-source projects—the
sizes and shapes of their files and of the changes developers actually make—rather than from
worst-case analysis alone.

This paper makes the following contributions:
\begin{itemize}
  \item It reports an empirical study of \NumRepos{} real-world open-source repositories, showing
  that the worst-case inputs tree-diffing algorithms are analyzed against are rare in practice,
  but that the tail is real and large enough that a production tool must handle inputs beyond
  what a whole-tree tree-edit-distance computation can process within an interactive budget.
  \item It reports a quantified comparison of four established diffing
  tools—GumTree, Unix \texttt{diff}, difftastic, and diffsitter—against human-authored
  ground-truth mappings of \NumFixtures{} real-world code changes.
  \item It reports a leave-one-out ablation study identifying which shapes of change require a
  dedicated matching heuristic, and showing that a heuristic's published benefit does not
  automatically transfer into a different pipeline evaluated against a different corpus.
  \item It details \textsc{CodeDiff}, a \NumPhasesWord{}-phase diffing pipeline that combines
  APTED's exact tree edit distance, GumTree's source-code-specific heuristics, and Myers'
  sequence diff into one production-engineered tool meeting the targets the empirical study sets.
\end{itemize}

\section{Background}
\label{sec:background}

This section introduces the algorithms and tools the rest of the paper depends on: the
algorithms \textsc{CodeDiff} builds its pipeline from, and the tools it is evaluated alongside.
Section~\ref{sec:related} returns to the closest prior work in more depth once the paper's own
results are in hand.

\subsection{Algorithms}

\textbf{Zhang-Shasha}~\cite{zhang-shasha-1989} gives the classic dynamic-programming algorithm for
ordered tree edit distance. Given two rooted, ordered trees, it computes the minimum-cost sequence
of node insertions, deletions, and relabelings that transforms one tree into the other, together
with the mapping that realizes that cost. It is exact, but its keyroot decomposition does
repeated work across overlapping subproblems. Later algorithms reduce this overhead while keeping
the same exact result.

\textbf{APTED}~\cite{apted-2015} is the state-of-the-art exact tree-edit-distance algorithm. It
improves on Zhang-Shasha by choosing, for each subproblem, the cheapest of three decomposition
strategies (left path, right path, or heaviest inner path), rather than a single fixed strategy.
This choice provably minimizes the total work across the whole computation. APTED still costs
$O(n^3)$ in the worst case for general trees, the same asymptotic bound as Zhang-Shasha, but its
real-world runtime is markedly lower, because most real trees are far from the adversarial shape
that bound assumes.

\textbf{GumTree}~\cite{gumtreediff-2014} targets source code directly, not general trees. Instead
of one global tree-edit-distance computation, it runs a top-down phase that greedily matches large
isomorphic subtrees, then a bottom-up phase that matches remaining container nodes by the Dice
coefficient of their already-matched descendants. GumTree also introduces a distinct edit
operation, move, and a recovery pass that pairs up deleted and inserted identical subtrees the
top-down and bottom-up phases missed. This design favors speed over exactness, at the scale real
source files require.

\textbf{Myers' O(ND) algorithm}~\cite{myers-1986} solves a different, more restricted problem: the
shortest edit script between two flat sequences, not two trees. It underlies most line-based diff
tools, including Unix \texttt{diff}. Its cost scales with the size of the edit script, not the size
of the inputs, so it stays fast when two sequences are similar, exactly the common case for a
version-controlled file.

\subsection{Tools}

\textbf{difftastic}~\cite{difftastic} and \textbf{diffsitter}~\cite{diffsitter} are a newer
generation of practical, tree-sitter-based diffing tools, the same parsing layer
\textsc{CodeDiff} itself is built on. Both prioritize a diff a human reads comfortably over an
exact node-to-node mapping: difftastic reduces the parse tree to an s-expression and aligns it
with its own structural algorithm, and diffsitter applies a related tree-sitter-based alignment
over a smaller, hand-picked set of compiled-in grammars. Neither computes a full tree-edit-distance
mapping the way APTED or GumTree do, so neither directly targets the ground-truth-accuracy metric
this paper measures against in Section~\ref{sec:evaluation}—a difference in design goal, not a
shortcoming, and the reason we include both as companions in the same problem space rather than as
head-to-head competitors on identical terms.

\section{Problem Space}
\label{sec:problem-space}

Formal treatments of the algorithms in Section~\ref{sec:background} report worst-case asymptotic
complexity: APTED and Zhang-Shasha are both $O(n^3)$ for general trees, where $n$ is tree size.
This analysis is necessary, but it is not sufficient for a production tool. GumTree's own
documentation states that it targets research use, not production deployment at scale—a
reasonable scope for its own goals, but one that leaves open a question every production diff
tool must answer:

\textbf{RQ1 (Scale).} What percentage of real-world source-code changes can a single, whole-tree
tree-edit-distance computation—the algorithmic core every exact, syntax-aware diff tool is built
on—complete within a one-second budget?

A tool engineered only against a worst-case bound risks two failure modes. It can over-engineer
for inputs that never occur in practice. It can under-engineer for a real tail its asymptotic
analysis never quantified. Answering RQ1 needs measurement, not further asymptotic analysis.

We measure this directly. We built a corpus of \NumRepos{} open-source repositories, drawn from
the Gentoo Linux distribution's package list and popular repositories on GitHub. We cloned every
repository in full and parsed every file with Tree-sitter, recording byte size, line count,
language, and AST node count per file. The full methodology, and every per-language number, is
reproducible from the project's research repository; here, we summarize the numbers that shape
\textsc{CodeDiff}'s design.

%% TODO: \NumRepos{} etc. currently reflect the 100-repository `small` sample (fast, already
%% computed), not the target 7,423-repository full corpus (slow - see README.md). Re-run
%% `make introductory-paper-empirical` after a full `file-stats` run to swap in the real numbers.
%%
%% TODO: \NumFixtures{} (98) and every number measured on that corpus - the ablation deltas, the
%% line-level comparison, the speed percentiles, the AST-node accuracy - predate substantial
%% pipeline work. research/optimal_solutions_benchmark.csv now holds 433 rows and
%% research/benchmark_other.csv 156, so the corpus has grown well past 98. These must be
%% refreshed together, not piecemeal: they were all measured on the same corpus, and updating one
%% block alone would leave the paper internally inconsistent. See paper_variables.py's AUTHORED
%% blocks for the command that regenerates each.

The corpus has \NumFiles{} files across \NumLanguages{} languages. About two-thirds of all files
are code. The rest is configuration, data, and documentation (Figure~\ref{fig:tips}). Restricted to
code files, Table~\ref{tab:file-size-percentiles} reports size percentiles in bytes, lines of code,
and AST nodes.

\begin{figure}
  \centering
  \includegraphics[width=0.9\linewidth]{figures/tips.png}
  \caption{Distribution of file types across the corpus.}
  \label{fig:tips}
\end{figure}

\begin{table}
  \caption{Per-file size percentiles across all languages, code files only.}
  \label{tab:file-size-percentiles}
  \begin{tabular}{lrrrrr}
    \toprule
    Metric & p50 & p90 & p99 & p99.9 & Max \\
    \midrule
    Bytes      & \BytesPFifty{} & \BytesPNinety{} & \BytesPNinetyNine{} & \BytesPNineNineNine{} & \BytesMax{} \\
    LOC        & \LocPFifty{}   & \LocPNinety{}   & \LocPNinetyNine{}   & \LocPNineNineNine{}   & \LocMax{} \\
    AST nodes  & \AstPFifty{}   & \AstPNinety{}   & \AstPNinetyNine{}   & \AstPNineNineNine{}   & \AstMax{} \\
    \bottomrule
  \end{tabular}
\end{table}

The largest file in the corpus has \AstMax{} AST nodes. Byte size and AST node count have a strong
correlation, Pearson $r = \CorrelationR{}$, which simplifies to
$|\text{AST}| \approx \text{bytes} / 5$ for quick estimates. Arafat and Riehle report that 47.5\%
of all commits touch 10 lines or fewer~\cite{commit-size-distribution-paper}, while commits of
over 10{,}000 lines still occur.

\textbf{Measurement.} To answer RQ1's percentage directly, we sampled \RqOnePairsAttempted{} real
(before, after) file pairs from single-parent commits in these repositories, stratified per
language and per size bucket so that large files are represented rather than drowned out by the
far more common small ones. We ran a single whole-tree tree-edit-distance computation on every
pair—\textsc{CodeDiff}'s own APTED implementation, applied to the full before and after trees
with none of \textsc{CodeDiff}'s pipeline heuristics—under a hard one-second budget enforced by
terminating the computation's process. This implementation includes an optimization that makes it
faster than a textbook APTED, so the failure rates below are a lower bound on how often a
whole-tree computation misses the budget, not an upper bound.

Figure~\ref{fig:rq1} shows the fraction of pairs completed within one second, per size bucket,
split by what kind of artifact the file is: code (\RqOneCodePairs{} pairs across general-purpose
programming languages), and configuration, data, and markup formats (\RqOneConfigDataPairs{}
pairs). The split matters because the two populations behave differently—blended, the headline
number would move with how many configuration files a sample happens to contain, independently of
anything the algorithm does to source code.

\begin{figure*}
  \centering
  \includegraphics[width=0.85\linewidth]{figures/apted_only_rq1_by_category.png}
  \caption{Fraction of sampled file pairs a whole-tree tree-edit-distance computation completes
  within a one-second budget, per size bucket and artifact category.}
  \label{fig:rq1}
\end{figure*}

\textbf{RA1.} For source code, a whole-tree computation is dependable only for small files: it
completes within one second for \RqOneCodeTenToThirtyPct{}\% of code pairs of 10--30 lines, but
for only \RqOneCodeHundredToThreeHundredPct{}\% of code pairs of 100--300 lines—well inside the
range of ordinary source files (Table~\ref{tab:file-size-percentiles})—and never recovers above
one third in any larger bucket. Across all sampled code pairs, \RqOneCodePct{}\% complete in
time; this sample-weighted aggregate deliberately over-represents large files and therefore
understates the rate a uniform sample of real commits would show, but the per-bucket rates carry
the answer regardless of weighting. The common case is genuinely cheap—most files and most
commits are small—but the tail is real: files up to \AstMax{} AST nodes occur in this corpus, and
Arafat and Riehle's data shows commits of over 10{,}000 lines occur in practice. A production
diff tool must be engineered for both regimes, and cannot rely on a whole-tree computation alone
for either. Section~\ref{sec:pipeline} turns this answer into \textsc{CodeDiff}'s concrete
design targets.

\section{CodeDiff}
\label{sec:pipeline}

The measurements in Section~\ref{sec:problem-space} give \textsc{CodeDiff} two concrete, named
design targets, stated in place of an asymptotic bound. \textbf{Robust}: \textsc{CodeDiff} must
handle every file up to the measured maximum, \AstMax{} AST nodes, with headroom above that number.
\textbf{Fast}: \textsc{CodeDiff} must compute a diff in under \SpeedTargetMs{}\,ms for \SpeedTargetPct{}\% of real-world
commits, the range that covers the overwhelming majority of commits per Arafat and Riehle's
measurement. Section~\ref{sec:evaluation} reports how well \textsc{CodeDiff} meets both targets in
practice.

\textsc{CodeDiff} matches two ASTs in \NumPhasesWord{} phases. Each phase either resolves part of the
mapping directly or narrows the residual left for the next phase. Later phases only ever see
nodes earlier phases left unmatched.

\textbf{Phase 1, hash-based descent.} \textsc{CodeDiff} matches subtrees by three hash variants,
in order: byte-identical subtrees, then same-shape subtrees with any leaf value, then import
statements matched by normalized path. Each variant claims what it can before the next runs. This
phase is \textsc{CodeDiff}'s own design, motivated directly by Section~\ref{sec:problem-space}'s
commit-size data: since most commits touch a small fraction of a file's lines, most of a typical
file's AST is byte-identical between before and after. Resolving that identical majority with cheap
hashing, before any tree-edit-distance computation runs at all, avoids paying that computation's
cost on content that never changed.

\textbf{Phase 2, contextual exact matching.} Comments that immediately precede an already-matched
node, and diagnostic statements (logging calls, assertions, panics) that are byte-identical on
both sides, get matched directly. Both are cheap, high-confidence matches that reduce the work
left for the tree-edit-distance phases below.

\textbf{Phases 3 and 5, bottom-up expansion.} This is GumTree's bottom-up matching heuristic: once
most of an unmatched node's descendants already match the descendants of some unmatched node on
the other side, by a Dice-coefficient threshold, match the two nodes to each other too, even
though nothing matched their own labels directly. \textsc{CodeDiff} runs this pass twice, once
before phase 4 and once after, since phase 4 produces more matched descendants for the second pass
to vote on. Section~\ref{sec:evaluation} reports that, once combined with the rest of this specific
pipeline, this heuristic does not carry over the accuracy gain GumTree's own evaluation found for
it, so \textsc{CodeDiff} ships it disabled by default—a property of this pipeline's measured
results, not of the heuristic's origin.

\textbf{Phase 4, syntax-aware subtree matching.} This phase generalizes three matching strategies
into one engine: matching by fully-resolved name (handling overloads and duplicate names across
scopes), a greedy cost-estimate matcher for containers with no name or arm structure, such as an
\texttt{if} block or a loop body, and a Jaccard-similarity matcher for flow-control constructs
whose case arms mostly agree. Before any of these run, large flat subtrees, such as a macro's
token list or a big flat argument list, are pre-matched directly with Myers'
algorithm~\cite{myers-1986}, since a flat sequence has no tree structure worth exploiting—exactly
the structures that drive Section~\ref{sec:problem-space}'s measured AST-size tail. Each accepted
pair is then handed to APTED (below) to compute its exact internal edit script.

\textbf{Phase 6, final APTED.} Whatever remains unmatched after phases 1 through 5 is handed to
APTED~\cite{apted-2015} for one whole-residual tree-edit-distance computation, giving an exact,
cost-minimal mapping over that remainder. When the residual is too large for this to finish inside
\textsc{CodeDiff}'s speed target, a cheaper Myers-LCS alignment~\cite{myers-1986} over the residual
substitutes for full APTED instead, trading a small amount of match quality for a bounded
worst-case latency. This fallback exists directly because of the measured size tail in
Section~\ref{sec:problem-space}: without it, the rare very large residual blows the \SpeedTargetMs{}\,ms target
outright.

\textbf{Phase 7, move-detection recovery.} The final pass. Ordered tree edit distance cannot
express a subtree that relocated across a matched boundary as anything but a delete plus an
insert. This phase, adapted directly from GumTree's recovery-mappings
phase~\cite{gumtreediff-2014}, pairs up whole subtrees left fully deleted on one side and fully
inserted on the other, when they are byte-identical and large enough to rule out coincidence.
Unlike phase 3/5's bottom-up heuristic, Section~\ref{sec:evaluation} finds this heuristic
measurably helps, so it ships enabled by default.

Zhang-Shasha~\cite{zhang-shasha-1989} plays no role in this pipeline as a shipped algorithm.
\textsc{CodeDiff}'s own test suite instead implements it from scratch as an independent oracle,
and fuzz-tests thousands of random trees to confirm that APTED's result always matches
Zhang-Shasha's, bit for bit. This checks the pipeline's most correctness-critical component
against a second, independently-implemented ground truth, not only against itself.

\section{Evaluation}
\label{sec:evaluation}

%% TODO: when the evaluation numbers are refreshed to the current corpus (see the TODO at the
%% top of this section's macros), also add a change-shape distribution table from
%% data/quality/human_mapping_analysis.csv (analysis/human_mapping_shapes_report.py) - RQ3's
%% "which shapes occur" half currently rests on the ablation alone, and the shape census must be
%% measured on the SAME corpus version as the rest of this section, which is why it is not added
%% now (the analysis file on disk covers 417 fixtures against this section's 98).
\textbf{Dataset.} \textsc{CodeDiff}'s accuracy claims rest on a corpus of \NumFixtures{} fixtures. Each fixture
pairs a before and after version of a source file, representing a realistic code change—a bug
fix, a refactor, an added feature, or a performance-motivated rewrite—across many languages. Some
fixtures are captured directly from a specific commit in a real open-source repository. Others are
curated examples of a common, realistic change pattern. For every fixture, a human annotator used a
purpose-built tool, \texttt{human\_solver}, to walk the before and after syntax trees side by side
and label every node with one of seven operations: identical, update, match-but-not-identical,
insert, insert-with-children, delete, and delete-with-children. Nodes are addressed by a path of
kind and sibling position, not by parser-internal node identity, since that identity is not stable
across separate parses of the same file. This human-authored mapping is the ground truth every
number in this section is measured against.

This section answers two further research questions, building on RQ1's finding that a real,
non-negligible tail of large files and commits exists alongside a small common case, and then
evaluates \textsc{CodeDiff} itself—the paper's system contribution—against the design targets
stated in Section~\ref{sec:pipeline}.

\textbf{RQ2 (Tool accuracy).} How accurately do current state-of-the-art code-diffing tools
recover real-world code changes, measured against human-authored ground-truth mappings of those
changes?

\textbf{RQ3 (Shapes and heuristics).} Which shapes of real-world
change—renames, moves, restructurings—require a dedicated matching heuristic, and do heuristics
shown effective in one pipeline and corpus retain that value when reused in a different pipeline
and evaluated against a different corpus?

\textbf{Comparison tools.} We place \textsc{CodeDiff} alongside four established tools, on the same
\NumFixtures{}-fixture corpus: GumTree (\GumTreeVersion{})~\cite{gumtreediff-2014}, Unix \texttt{diff}, and the two
tree-sitter-based tools from Section~\ref{sec:background}, difftastic~\cite{difftastic} and
diffsitter~\cite{diffsitter}.

This comparison measures one specific property: agreement with the human-authored ground-truth
mapping above. difftastic and diffsitter deliberately optimize for something this metric does not
capture, a diff that reads comfortably to a human, not a mapping that reproduces a ground truth
line for line, so a lower score here reflects a different design goal, not a defect. GumTree
remains the foundation two of \textsc{CodeDiff}'s own pipeline phases build on directly
(Section~\ref{sec:pipeline}), and its broader language and backend flexibility stays valuable well
beyond what this single corpus measures. Read what follows as a report of where combining these
approaches into one production-engineered, ablation-validated pipeline lands, not as a ranking of
which tool is best.

\textbf{AST-node accuracy.} Measured directly against the human mapping, \textsc{CodeDiff} maps
\NodesMatched{} of \NodesTotal{} AST nodes correctly across the \NumFixtures{} fixtures, \NodeMismatches{} mismatches, or \NodeAccuracyPct{}\%
node-level accuracy.

\textbf{Line-level agreement.} None of the other four tools report an AST-node mapping, so we
compare all five on a common ground: which source lines each tool marks as touched, against the
same human mapping (Table~\ref{tab:accuracy-vs-others}, Figure~\ref{fig:accuracy}). Coverage
varies: diffsitter's compiled-in grammar set matches \DiffsitterFixtures{} of the \NumFixtures{} fixtures' languages, GumTree's
generator set matches \GumTreeFixtures{}, and the rest match all \NumFixtures{}—each tool's own row is scored only on its own
applicable subset, not padded with unscored fixtures.

\textbf{RA2.} Among the four pre-existing tools (Table~\ref{tab:accuracy-vs-others}, excluding
\textsc{CodeDiff}'s own row), line-level mismatch rates against the human mapping range from
\UnixDiffLineRate{}\% (Unix \texttt{diff}) to \DiffsitterLineRate{}\% (diffsitter). Two results
stand out. First, every tool already agrees with the human mapping on the overwhelming majority
of lines: even the highest mismatch rate leaves more than 98\% of lines in agreement, so the
question that separates tools is how they handle the small, structurally interesting remainder.
Second, no AST-aware tool among the four beats plain line-based \texttt{diff} on this measure.
Syntax-awareness alone does not guarantee that a tool recovers a change the way a human reads
it—the gap is in which heuristics act on the syntax tree, which RQ3 turns to next.

\begin{table}
  \caption{Line-level mismatches against human ground truth, each tool scored on its own
  applicable subset of the \NumFixtures{}-fixture corpus.}
  \label{tab:accuracy-vs-others}
  \begin{tabular}{lrrr}
    \toprule
    Tool & Fixtures & Mismatched lines & Rate \\
    \midrule
    \textsc{CodeDiff} & \CodeDiffFixtures{} & \CodeDiffLineMismatches{} & \CodeDiffLineRate{}\% \\
    Unix \texttt{diff} & \UnixDiffFixtures{} & \UnixDiffLineMismatches{} & \UnixDiffLineRate{}\% \\
    GumTree & \GumTreeFixtures{} & \GumTreeLineMismatches{} & \GumTreeLineRate{}\% \\
    difftastic & \DifftasticFixtures{} & \DifftasticLineMismatches{} & \DifftasticLineRate{}\% \\
    diffsitter & \DiffsitterFixtures{} & \DiffsitterLineMismatches{} & \DiffsitterLineRate{}\% \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure*}
  \centering
  \includegraphics[width=0.85\linewidth]{figures/benchmark_other_accuracy.png}
  \caption{Line-level agreement rate with the human mapping, bucketed in 10-point increments, all
  five tools on the same axis. Each series' own $n$ reflects its applicable subset of the corpus.}
  \label{fig:accuracy}
\end{figure*}

\textbf{Ablation study.} RQ3 asks which shapes of change earn a dedicated heuristic.
\textsc{CodeDiff} has four optional heuristic passes, each targeting a specific shape and each
behind its own flag: import-node normalization (renamed or reordered import paths), flow-control
arm matching (restructured \texttt{if}/\texttt{switch} arms), bottom-up expansion (containers
whose children already match, phases 3 and 5), and move-detection recovery (whole subtrees
relocated across a matched boundary, phase 7). A leave-one-out ablation study measured each
pass's real contribution on the \NumFixtures{}-fixture corpus above, by disabling one pass at a
time and comparing the resulting mismatch count against a baseline with all four enabled.

Table~\ref{tab:ablation} reports the result. Import-node normalization and flow-control arm
matching, two of \textsc{CodeDiff}'s own heuristics, both turned out net-negative for accuracy on
this corpus once combined with the rest of the pipeline. Bottom-up expansion, adapted from
GumTree, shows the same pattern here—not because the technique is unsound (GumTree's own
evaluation, on GumTree's own pipeline and corpus, found it valuable), but because a heuristic's
net effect depends on what the rest of the pipeline already resolves before that heuristic gets
its turn, and \textsc{CodeDiff}'s earlier phases leave GumTree's bottom-up phase less new ground
to help with. Move-detection recovery, GumTree's other contribution to this pipeline, is the one
of the four that does carry over: it measurably helps here too, and ships enabled by default. The
other three ship disabled.

\begin{table}
  \caption{Ablation study: accuracy change from disabling each optional pass, against a baseline
  with all four enabled. A negative number means disabling the pass improved accuracy, fewer
  mismatches. A positive number means disabling it hurt accuracy. The two passes GumTree
  contributes are marked (G).}
  \label{tab:ablation}
  \begin{tabular}{lrl}
    \toprule
    Pass & $\Delta$ if disabled & Ships \\
    \midrule
    Import-node normalization & $\AblationImportNodes{}$ & disabled \\
    Flow-control arm matching & $\AblationFlowControl{}$ & disabled \\
    Bottom-up expansion (G)   & $\AblationBottomUp{}$ & disabled \\
    Move-detection recovery (G) & $\AblationMovedSubtrees{}$ & enabled \\
    \bottomrule
  \end{tabular}
\end{table}

\textbf{RA3.} On this corpus, the move is the one shape whose dedicated heuristic pays for
itself: move-detection recovery is the only optional pass whose removal hurts accuracy, which
matches the structural argument of Section~\ref{sec:pipeline}—a relocated subtree is the one
shape ordered tree edit distance cannot express except as a delete plus an insert, so no
downstream computation can recover it without a dedicated pass. The heuristics targeting the
other shapes are net-negative here, including bottom-up expansion, which GumTree's own
evaluation, on GumTree's own pipeline and corpus, validated as helpful. A technique documented as
effective in its original paper is therefore not guaranteed to help inside a different pipeline
evaluated against a different corpus. \textsc{CodeDiff} measures this directly for every optional
pass, rather than assuming a result from the literature transfers unchanged.

\textbf{Production viability.} The remaining measurements evaluate \textsc{CodeDiff}
itself—speed, then robustness—against the Fast and Robust targets stated in
Section~\ref{sec:pipeline}.

\textbf{Speed.} Table~\ref{tab:speed-vs-others} and Figure~\ref{fig:runtime} report wall-clock
percentiles over the same fixtures, sorted fastest to slowest by median. We report GumTree twice:
once invoked as a fresh process per fixture, the realistic cost of a single CLI invocation, and
once run inside one persistent JVM across all fixtures, isolating JVM startup from GumTree's own
matching cost. diffsitter is the fastest AST-aware tool in this comparison at every percentile
measured, a direct result of its narrower, hand-picked grammar set. difftastic sits close to
\textsc{CodeDiff} at the median and 90th percentile, but its tail is heavier: at the 99th
percentile, difftastic is the slowest AST-aware tool here after GumTree invoked as a fresh process.
Unix \texttt{diff} remains fastest overall at every percentile, since it never parses or compares
tree structure.

These percentiles must be read against the right population. The Fast target of
Section~\ref{sec:pipeline}—\SpeedTargetMs{}\,ms for \SpeedTargetPct{}\% of real-world
commits—is a claim about the real distribution of commits, in which 47.5\% touch 10 lines or
fewer~\cite{commit-size-distribution-paper}. The fixture corpus is not that distribution: it
deliberately concentrates hard, structurally interesting changes, because easy changes exercise
nothing worth measuring. \textsc{CodeDiff}'s 99th percentile on this corpus
(\SpeedCodeDiffPNinetyNine{}\,ms) is therefore a stress figure on an adversarially weighted
sample, not the target metric; its median (\SpeedCodeDiffPFifty{}\,ms) and 90th percentile
(\SpeedCodeDiffPNinety{}\,ms) on the same hard sample sit well inside the budget. Validating the
\SpeedTargetPct{}\% claim on a uniformly drawn commit sample remains future work, and we state it
here as a design target rather than a measured result.

\begin{table}
  \caption{Wall-clock time percentiles, milliseconds, sorted fastest to slowest by median.}
  \label{tab:speed-vs-others}
  \begin{tabular}{lrrr}
    \toprule
    Tool & p50 & p90 & p99 \\
    \midrule
    Unix \texttt{diff} & \SpeedUnixDiffPFifty{} & \SpeedUnixDiffPNinety{} & \SpeedUnixDiffPNinetyNine{} \\
    diffsitter & \SpeedDiffsitterPFifty{} & \SpeedDiffsitterPNinety{} & \SpeedDiffsitterPNinetyNine{} \\
    \textsc{CodeDiff} & \SpeedCodeDiffPFifty{} & \SpeedCodeDiffPNinety{} & \SpeedCodeDiffPNinetyNine{} \\
    GumTree (warm JVM) & \SpeedGumTreeWarmPFifty{} & \SpeedGumTreeWarmPNinety{} & \SpeedGumTreeWarmPNinetyNine{} \\
    difftastic & \SpeedDifftasticPFifty{} & \SpeedDifftasticPNinety{} & \SpeedDifftasticPNinetyNine{} \\
    GumTree (cold, per-invocation) & \SpeedGumTreeColdPFifty{} & \SpeedGumTreeColdPNinety{} & \SpeedGumTreeColdPNinetyNine{} \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure*}
  \centering
  \includegraphics[width=0.85\linewidth]{figures/benchmark_other_runtime.png}
  \caption{Full per-fixture runtime distribution (log scale), every individual repeated
  measurement plotted, alongside a tree-sitter-parse-only reference violin (the lower bound every
  AST-aware tool here must pay before its own work even starts).}
  \label{fig:runtime}
\end{figure*}

Table~\ref{tab:variance} reports a companion measurement: how much any single timing run can be
trusted, the per-fixture coefficient of variation across repeated measurements of the same
fixture. This is what motivated repeating every timing number in this paper across multiple runs
rather than trusting a single measurement.

\input{figures/benchmark_other_variance.tex}

\textbf{Robustness.} We sampled \RobustnessSampled{} real (repository, commit, file) pairs from real Rust
repositories and ran every pair through \textsc{CodeDiff}, up to a \RobustnessNodeCap{} combined AST-node cap
and a \RobustnessTimeoutSeconds{}-second timeout per pair. Of the \RobustnessSampled{} sampled pairs, \RobustnessUnavailable{} were unavailable locally
because the repository's history had since been rewritten, not a \textsc{CodeDiff} failure, and
\RobustnessSkipped{} exceeded the node cap and were skipped before \textsc{CodeDiff} ever ran. \textsc{CodeDiff}
completed all \RobustnessCompleted{} remaining pairs with zero panics and zero timeouts. This is a sampled,
bounded-scale result, not a claim over the full \NumRepos{}-repository corpus, but it is
consistent with \textsc{CodeDiff}'s stated Robust target in Section~\ref{sec:pipeline}.

\textbf{Summary.} Combining established techniques rather than inventing new ones,
\textsc{CodeDiff} reaches \NodeAccuracyPct{}\% AST-node accuracy against human-verified ground truth, the lowest
line-level mismatch rate of the five tools compared, wall-clock latency faster than GumTree and
difftastic at the median, and zero panics or timeouts across the sampled robustness evaluation.
Building a production-grade syntax-aware diffing tool did not require a new matching algorithm,
only careful combination and measurement of existing ones.

\section{Related Work}
\label{sec:related}

\textbf{Tree-diffing tools.} GumTree~\cite{gumtreediff-2014} remains the reference point for
source-code tree diffing, and two of \textsc{CodeDiff}'s pipeline phases adapt its heuristics
directly (Section~\ref{sec:pipeline}). The difference is one of goal: GumTree targets research
use, with broad language and backend flexibility, while \textsc{CodeDiff} narrows to a
tree-sitter-only frontend and spends the saved generality on production targets—and, as the
ablation in Section~\ref{sec:evaluation} shows, on validating each adopted heuristic inside its
own pipeline rather than assuming the published benefit transfers. difftastic~\cite{difftastic}
and diffsitter~\cite{diffsitter} share \textsc{CodeDiff}'s parsing layer but optimize for a diff
a human reads comfortably rather than for fidelity of the underlying node mapping; RA2 reflects
that difference in design goal, not a defect in either tool. Unix \texttt{diff}, built on Myers'
algorithm~\cite{myers-1986}, remains the baseline every one of these tools must justify itself
against—on our corpus it is both the fastest tool measured and, at the line level, more accurate
than every pre-existing AST-aware tool compared.

\textbf{Empirical studies.} Dyer et al.~\cite{ast-api-1} and L\"{a}mmel et al.~\cite{ast-api-2}
mine AST nodes at scale to study API usage; Arafat and
Riehle~\cite{commit-size-distribution-paper} measure the commit-size distribution of open-source
software, the distribution \textsc{CodeDiff}'s Fast target is stated against. This paper's
empirical study differs from these in purpose rather than method: it measures file- and AST-size
distributions specifically to set, and then evaluate, the engineering targets of a diffing tool.

\section{Conclusion and Future Work}
\label{sec:conclusion}

None of \textsc{CodeDiff}'s matching algorithms are new. Its contribution is combining
Zhang-Shasha's and APTED's exact tree edit distance, GumTree's source-code-specific heuristics, and
Myers' sequence diff into a single pipeline, engineered and measured against the real distribution
of source code rather than an assumed worst case, and validating every optional piece of that
combination by measurement instead of by assumption. On the corpus in this paper, that combination
reaches an accuracy, speed, and reliability comparable to or exceeding each individual predecessor
it draws from, without displacing what any of them are independently good at: GumTree's language
and backend flexibility, difftastic's and diffsitter's speed and readability, Unix \texttt{diff}'s
universality. The ablation result in Section~\ref{sec:evaluation} is the more general, transferable
point: a heuristic's value is a property of the pipeline and corpus it runs in, not only of the
paper that introduced it. A production tool must measure that value directly, rather than assume
it carries over.

Based on the lessons reported in this paper, we recommend the following to builders of
syntax-aware developer tooling:
\begin{itemize}
  \item Measure every adopted heuristic inside your own pipeline, against your own corpus: of the
  two GumTree heuristics adopted here, one helped and one hurt, and only direct measurement told
  them apart.
  \item Engineer against the measured distribution of real inputs, not against the worst-case
  bound alone: the common case and the tail require different machinery, and both occur in
  practice.
  \item Keep an exact algorithm as an independent test oracle even when the shipped path is
  heuristic: \textsc{CodeDiff} fuzz-checks its APTED implementation against an independently
  implemented Zhang-Shasha on thousands of random trees.
\end{itemize}

Future work includes widening the ground-truth and robustness corpora beyond Rust and the current
\NumFixtures{} fixtures, extending the accuracy comparison to more languages GumTree also supports,
re-running the ablation study as new heuristic passes are added to the pipeline, and quantifying
the distribution of change shapes in the ground-truth corpus itself, so that RQ3's shape findings
rest on measured frequencies rather than on the ablation alone.

\bibliographystyle{ACM-Reference-Format}
\bibliography{references}

\end{document}
