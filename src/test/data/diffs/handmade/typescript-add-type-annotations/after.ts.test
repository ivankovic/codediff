interface User {
    name: string;
    age: number;
    createdAt: Date;
}

function addUser(name: string, age: number): User {
    return {
        name: name,
        age: age,
        createdAt: new Date()
    };
}

const user: User = addUser("Alice", 30);
console.log(user);
