function fetchData() {
    return new Promise((resolve) => {
        setTimeout(() => {
            resolve("data");
        }, 1000);
    });
}

fetchData().then((data) => {
    console.log(data);
});
