fn find_duplicate(nums: &[i32]) -> Option<i32> {
    // Brute force approach
    for i in 0..nums.len() {
        for j in i+1..nums.len() {
            if nums[i] == nums[j] {
                return Some(nums[i]);
            }
        }
    }
    None
}

fn main() {
    let nums = vec![1, 3, 4, 2, 2];
    if let Some(dup) = find_duplicate(&nums) {
        println!("Duplicate found: {}", dup);
    } else {
        println!("No duplicate found");
    }
}