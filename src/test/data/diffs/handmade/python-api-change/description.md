API change and enhancement: Updated API client code with better error handling, type hints, and new functionality. This represents a common evolution of API client code.

Key changes:
- Added type hints throughout (`int`, `Optional`, `Dict`, etc.)
- Updated API version from v1 to v2 in URL
- Added timeout parameter to fetch_user_data
- Improved error handling with try/except and raise_for_status()
- Added new function get_user_info() for comprehensive user data
- Enhanced return types and error reporting

This shows how API client code evolves to handle new API versions and becomes more robust over time.