Code refactoring: Replaced manual loops with Python built-in functions for cleaner, more idiomatic code. This is a common Python refactoring pattern.

Key changes:
- Replaced manual sum loop with `sum(numbers)`
- Replaced manual count loop with `len(numbers)`
- Replaced manual max/min loops with `max(numbers)` and `min(numbers)`
- Added 'total' to the returned statistics dictionary
- Removed redundant loops and simplified logic

This represents how Python code often evolves to become more idiomatic by leveraging built-in functions.